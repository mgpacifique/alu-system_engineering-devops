#!/usr/bin/env bash
# Configure Ubuntu machine with nginx, redirect, and custom 404 page

sudo apt-get update -y
sudo apt-get install -y nginx

# Default page
echo "Holberton School for the win!" | sudo tee /var/www/html/index.nginx-debian.html > /dev/null

# Custom 404 page
echo "Ceci n'est pas une page" | sudo tee /var/www/html/404.html > /dev/null

# Replace default nginx config completely (safe & clean)
cat > /etc/nginx/sites-available/default <<'EOF'
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.nginx-debian.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }

    location /redirect_me/ {
        return 301 https://www.youtube.com/watch?v=QH2-TGUlwu4;
    }

    error_page 404 /404.html;

    location = /404.html {
        internal;
    }
}
EOF

sudo service nginx restart